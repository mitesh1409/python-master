# Data Types

* String
* Number
    - Integer (for whole numbers)
    - Float (numbers with decimal part)
* Boolean

Boolean type supports only two values - `True` & `False`.  
These values are never quoted.  

Use built-in `type()` function to get the type of a variable in Python.  
